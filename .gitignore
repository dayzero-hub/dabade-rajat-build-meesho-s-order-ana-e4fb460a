# Python virtual environment
.venv/
venv/
__pycache__/

# dbt build artefacts
target/
dbt_packages/
logs/

# The DuckDB warehouse is generated by `dbt build` — never commit it
*.duckdb
*.duckdb.wal

# The raw CSVs are downloaded from Kaggle into seeds/raw/ (see README). They are NOT
# committed — the dataset's licence is not ours to redistribute. Only .gitkeep is tracked.
seeds/raw/*.csv
